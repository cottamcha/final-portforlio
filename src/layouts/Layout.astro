---
import LayoutHeader from "./LayoutHeader.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<LayoutHeader {title} {description}>
  <slot />
</LayoutHeader>
